{
    "builds": [{
        "src": "test_proj/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.8.2" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "test_proj/wsgi.py"
        }
    ]
}